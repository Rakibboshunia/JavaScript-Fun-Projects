// Sign Value Methods

        x = 5;
        y = -5;

        s = Math.sign(x); // Returns 1 if positive, -1 if negative, 0 if zero
        document.write(s + "<br>");

        s = Math.sign(y); // Returns 1 if positive, -1 if negative, 0 if zero
        document.write(s + "<br>");

        // Absolute Value Methods

        s = Math.abs(x); // Returns 1 if positive, -1 if negative, 0 if zero
        document.write(s + "<br>");

        s = Math.abs(y); // Returns 1 if positive, -1 if negative, 0 if zero
        document.write(s + "<br>");
